---
# handlers file for docker
  - name: "start docker service"
    ansible.builtin.service:
      name: docker
      state: started
      enabled: yes

  - name: restart docker
    ansible.builtin.service:
      name: docker
      state: restarted    